<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>OSAntiCheat.VisOracle</RootNamespace>
    <AssemblyName>osac-visoracle</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- Present so the engine-referencing plugin assembly loads at runtime. -->
    <PackageReference Include="CounterStrikeSharp.API" Version="1.0.*" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../../src/OSAntiCheat.csproj" />
  </ItemGroup>

</Project>
